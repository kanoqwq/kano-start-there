name: Release
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14'
      - name: Install dependencies
        run: npm install
      - name: Build and test
        run: npm run build && npm test

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      #- name: ls
      #  run: ls
      #- name: Zip the Build
      #  run: zip -r release.zip ./dist/
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            LICENSE
          #  release.zip
          tag_name: v${{ github.ref }}
          name: Release ${{ github.ref }}
          body: |
            Changes in this Release:
            - Add feature A
            - Fix issue B
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.githttps }}
